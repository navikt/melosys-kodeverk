const { begrunnelser } = require('./begrunnelser');
const { normalt_virksomhet } = require('./normalt_virksomhet');

module.exports = {
  art_12_2: {
    begrunnelser,
    normalt_virksomhet,
  },
};
