const { produserbaredokumenter } = require('./produserbaredokumenter');
const { brevtype } = require('./brevtype');

const brev = {
  produserbaredokumenter,
  brevtype
};

module.exports.brev = brev;
